---
version: 1.0
profiles:
  - base-developer
strict_override: false
---

# Workspace Instructions

## Overview
Project description

## Workspace Rules
- Ensure all skills added to `./skills/` are provider-agnostic.
- Use `./memory/` to store shared context about the framework's evolution.
- Use `./plan/` to track the implementation of the setup guide and initializers.
