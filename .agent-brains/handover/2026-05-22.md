## Session Handover — 2026-05-22

### Accomplished
- **Material elevation shadow removed** from `Button.TreeNodeRow` via `assists:ShadowAssist.ShadowDepth=Depth0` (from `Material.Styles.Assists`) plus a `/template/ Border { BoxShadow=transparent }` override. API was discovered by extracting strings from `Material.Styles.dll`.
- **PR #36 opened, reviewed, fixed, re-reviewed, and merged** in one session:
  - First `pr-review` flagged 3 🔴 critical + 6 ⚠️ warnings + 4 ℹ️ items.
  - Fixes (`f5be317`): `ClusterNodeViewModel` captures `SynchronizationContext.Current` with same-context shortcut, implements `IDisposable`; `ClusterTreeViewModel.RebuildTree` disposes outgoing nodes before clearing `Sources`; `ClusterConnectionManager.GetPortForwardService` returns nullable; `ClusterConnectionManager : IDisposable` stops port-forwards on shutdown; dead `AddClusterDialogViewModel` DI registration removed; `SelectableContext` extracted to its own file; `ErrorMessage_ClearedOnSuccessfulConnect` rewritten + new `Dispose_UnsubscribesFromManagerEvent` test.
  - Second `pr-review` returned **✅ approved**.
  - PR merged as squash commit `533bdf1` on main.
- **Workspace standard added** to `.agent-brains/AGENT.md`: VMs subscribing to Singleton events MUST implement `IDisposable`; parent VMs MUST dispose children before re-creating.
- **Memory note added** to `memory/overview.md`: xUnit `AsyncTestSyncContext` gotcha and the `SynchronizationContext.Current == _uiContext` shortcut pattern.

### Current State
- **Branch**: `main` at `533bdf1` (PR #36 merged). 3 uncommitted handover/memory/backlog edits ready to commit.
- **Active plan**: none. Backlog has no In Progress items.
- **Tests**: 78 passing (47 Core + 31 App), build clean with `-warnaserror`.

### Next Steps
1. Optional cleanup: delete the merged local branch `feature/multi-cluster-tree-nav` (and the many other stale local branches surfaced by `git branch -vv`).
2. Track as workspace-standard follow-up: convert remaining classes to primary constructor syntax + adopt `[LoggerMessage]` source-generated logging across the codebase. Deliberately deferred from PR #36 to keep its diff focused.
3. Consider re-cutting a release tag if `1.2.7 → 1.3.0` is appropriate (multi-cluster is a sizeable feature addition).

### Decisions Made
- **TreeView → nested `ItemsControl`**: chosen because Avalonia 12 doesn't expose `TreeViewItem.Indent` in style selectors. ADR skipped per user.
- **UI dispatch in `Core` via captured `SynchronizationContext`**: avoids dragging Avalonia into `Core`; same-thread shortcut keeps xUnit `AsyncTestSyncContext` tests synchronous. ADR skipped.
- **`GetPortForwardService` returns nullable** (fail-loud), rejecting the "create on demand" pattern that orphaned port-forwards. ADR skipped.

### Blockers / Notes
- None. PR is merged. Codebase is green.
- The `_disposed` re-check inside `ApplyStatusUpdate` and `LoadNamespacesAsync` is load-bearing — a posted continuation can run after `Dispose()` because the SynchronizationContext queue is FIFO and the unsubscribe doesn't drain pending posts.
